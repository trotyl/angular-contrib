{
  "name": "@angular-contrib/common",
  "version": "{{ version }}",
  "peerDependencies": {
    "@angular/common": "^{{ angularCompatVersion }}",
    "@angular/core": "^{{ angularCompatVersion }}"
  }
}
